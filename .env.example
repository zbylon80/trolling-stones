# Copy to `.env` and fill in deployment-specific secrets.
# Keep `.env` out of version control.

# Reserved for admin interface login; provide via secrets manager.
ADMIN_PASSWORD=

# Stage API used by the front-end. Override for other environments.
VITE_API_URL=https://trolling-stones-stage.onrender.com
